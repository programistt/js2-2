let ism = prompt("ismingizni kiriting :");
let id = prompt("id kiriting :");
let pass = prompt("passwordingizni kiriting :")

let data = {
    name: ism,
    id: id,
    parol: pass
};

console.log(data);